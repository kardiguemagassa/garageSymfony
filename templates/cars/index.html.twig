{% extends 'base.html.twig' %}

{% block title %}Nos Vehicules D'occasions{% endblock %}

{% block content %}
 
{# =============================================================================== #}
   {# FILTER #}
    <section class="row my-5 border-bottom border-warning">
        <h3 class="text-center text-warning my-5">Filtres</h3>

        {% include "cars/filter.html.twig" with {formSearch: formSearch} only %}

    </section>

{# ===============================================================================> #}

    {# Les vehicules #}
    {# <section class="services" id="services">

        <h1 class="heading"> <span>Nos véhicules</span> d'occasion </h1>
        
        <div class="box-container">
            {% for car in cars %}
                {% include 'cars/single_car.html.twig' %}
            {% endfor %}
        </div>

        <hr>
    
    </section> #}

    {# ==============================================================================> #}


    <section class="featured" id="featured">

        <div id="carsContainer">
            <h1 class="heading"> <span>Nos véhicules</span> d'occasion </h1>

            <div class="swiper featured-slider">
                <div class="swiper-wrapper">

                    {% for car in cars %}
                        {% include 'cars/single_car.html.twig' %}
                    {% endfor %}
                </div>

                <div class="swiper-pagination"></div>
            </div>

            <div class="swiper featured-slider">
                <div class="swiper-wrapper">

                    {% for car in cars %}
                        {% include 'cars/single_car.html.twig' %}
                    {% endfor %}
        
                </div>
        
                <div class="swiper-pagination"></div>
            </div>

            <p class="pagination ">{{ knp_pagination_render(cars) }}</p>
        </div>
    </section>

{% endblock %}

{# bug js, pour que la recherche symfony function il faut mettre en commantaire js ci-dessous#}
{% block javascript %}
	<script src="{{ asset('assets/js/filter.js') }}"></script>
{% endblock javascript %} 


